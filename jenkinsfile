pipeline {
    agent any
    stages {
        stage('Building Master branch'){
            when {
                branch 'master'
            }
            steps {
                mvn clean compile
            }
            
        stage ('Building develop branch') {
            when {
                branch 'develop'
            }
            steps {
                mvn clean install
            }
            
        }

    }
}